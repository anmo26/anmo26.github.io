Drop images in this folder — jpg, png, gif, webp, avif, svg.

They appear on the site at full size (capped to a readable width).

HEIC from an iPhone will NOT display in browsers. Convert first:
  sips -s format jpeg photo.heic --out photo.jpg
